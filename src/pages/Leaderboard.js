import React from "react";
import { Box } from "@mui/material";

const Leaderboard = () => {
    return (
        <Box>
            <h1> Leaderboard </h1>
        </Box>
    );
};

export default Leaderboard;